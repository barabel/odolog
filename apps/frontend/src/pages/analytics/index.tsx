import cx from 'classix';

export const AnalyticsPage: FCClass = ({
  className,
}) => {
  return (
    <div
      className={cx(
        't1',
        className,
      )}
    >
      AnalyticsPage
    </div>
  );
};

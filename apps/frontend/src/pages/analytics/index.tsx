import cx from 'classix';

export const AnalyticsPage: FCClass = ({
  className,
}) => {
  return (
    <div
      className={cx(
        't2',
        className,
      )}
    >
      AnalyticsPage
    </div>
  );
};

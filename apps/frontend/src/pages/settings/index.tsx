import cx from 'classix';

export const SettingsPage: FCClass = ({
  className,
}) => {
  return (
    <div
      className={cx(
        't1',
        className,
      )}
    >
      SettingsPage
    </div>
  );
};

# Icon

SVG-иконка из спрайта `/sprite.svg`. Рендерит `<svg>` с `<use href="/sprite.svg#...">`.

## Пропсы

- `icon` — название иконки из спрайта (обязательный)
- `width` — ширина, по умолчанию `20`
- `height` — высота, по умолчанию `20`
- `style` — инлайн-стили
- `ref` — реф на svg-элемент

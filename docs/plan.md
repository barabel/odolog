# Odolog — план разработки

## Контекст

Замена связки Telegram + Google Sheets для учёта пробега и расхода топлива.
Записи двух типов: показание одометра (раз в день) и заправка (когда мигает лампочка).

---

## Стек

| Слой | Технология |
|------|------------|
| Frontend | React + Vite + TypeScript + Tailwind CSS |
| Offline DB | Dexie.js (IndexedDB) |
| Backend | Node.js + Express + SQLite (better-sqlite3), отдаёт и API, и статику фронта |
| Auth | JWT Bearer |
| PWA | vite-plugin-pwa |

---

## Схема данных

```ts
// vehicles
{ id: string, name: string }

// odometer_entries
{ id: string, vehicle_id: string, date: string, odometer: number, synced: boolean, updated_at: number }

// fuel_entries
{ id: string, vehicle_id: string, date: string, odometer: number, liters: number, total_cost: number, synced: boolean, updated_at: number }
```

- `id` — UUID, генерируется на клиенте
- `date` — ISO строка (YYYY-MM-DD), по умолчанию сегодня, редактируемая
- `synced` — флаг локальной синхронизации
- `updated_at` — timestamp (ms), при конфликте побеждает большее значение

Вычисляемые поля (не хранятся): пробег между записями, расход л/100км, цена за литр.

---

## Синхронизация

Двусторонняя, сервер — источник истины.

1. Push: отправляем все записи где `synced = false`
2. Pull: тянем с сервера все записи новее `last_sync_at`
3. Конфликт по одному `id`: побеждает запись с бо́льшим `updated_at`
4. Авто-sync при появлении сети + кнопка ручного sync

---

## Архитектура

### Frontend — Feature-Sliced Design (FSD)
```
src/
  app/        # провайдеры, роутинг, глобальные стили
  pages/      # страницы
  widgets/    # составные блоки UI
  features/   # фичи (add-odometer-entry, add-fuel-entry, sync)
  entities/   # сущности (vehicle, odometer-entry, fuel-entry)
  shared/     # lib (db, api-client), ui (компоненты), types
```

### Backend — Onion Architecture
```
src/
  domain/         # сущности + интерфейсы репозиториев (чистый TS, без зависимостей)
  application/    # use cases (бизнес-логика)
  infrastructure/ # реализация репозиториев (SQLite), Express роуты, JWT
```

---

## Фазы

### Фаза 1 — Offline core
*Цель: записи сохраняются в IndexedDB, без сервера*

- [x] **Проект: Vite + React + TS + Tailwind**
  - [x] Запускается в dev-режиме
  - [x] TS компилируется без ошибок
  - [x] Tailwind-классы применяются

- [ ] **Dexie.js: схема таблиц + дефолтная машина**
  - [ ] Инстанс DB создан, схема (`vehicles`, `odometer_entries`, `fuel_entries`) определена
  - [ ] При первом запуске в `vehicles` автоматически создаётся `{ name: 'Мой автомобиль' }` (без UI)

- [ ] **Форма записи одометра** (дата = сегодня, редактируемая; показание одометра)
  - [ ] Форма рендерится
  - [ ] Дата и одометр обязательны, одометр > 0
  - [ ] После отправки запись сохраняется в Dexie с `id`, `synced: false`, `updated_at`
  - [ ] Форма сбрасывается или показывается подтверждение

- [ ] **Форма заправки** (дата, одометр, литры, общая стоимость)
  - [ ] Форма рендерится
  - [ ] Все 4 поля обязательны, числовые > 0
  - [ ] После отправки запись сохраняется в Dexie с `id`, `synced: false`, `updated_at`
  - [ ] Форма сбрасывается или показывается подтверждение

- [ ] **Список записей** (оба типа)
  - [ ] Оба типа отображаются, видны дата и значение
  - [ ] Типы визуально различимы
  - [ ] Сортировка по дате, новые сверху
  - [ ] Список обновляется автоматически после отправки формы

*UI: табы или формы друг под другом, вид списка — решить при реализации*

### Фаза 2 — Backend
*TODO: добавить критерии готовности для каждого шага*
*Цель: API с авторизацией, данные хранятся на сервере*

- [ ] Express + better-sqlite3, схема БД (те же таблицы)
- [ ] `express.static('dist')` + SPA fallback (`*` → `index.html`)
- [ ] `POST /auth/login` → JWT
- [ ] JWT middleware для защиты роутов
- [ ] CRUD для `odometer_entries`
- [ ] CRUD для `fuel_entries`

### Фаза 3 — Sync + PWA
*TODO: добавить критерии готовности для каждого шага*
*Цель: работает офлайн, устанавливается как приложение, синхронизируется*

- [ ] Форма логина + хранение JWT-токена (localStorage)
- [ ] vite-plugin-pwa: манифест, иконки, installable
- [ ] Service worker: кэш статики (offline режим)
- [ ] Sync: push несинхронизированных записей
- [ ] Sync: pull новых записей по `last_sync_at`
- [ ] Авто-sync при появлении сети
- [ ] Кнопка ручной синхронизации + индикатор статуса

### Фаза 4 — Analytics
*TODO: добавить критерии готовности для каждого шага*
*Цель: видеть расход и пробег, не считать вручную*

- [ ] Расчёт пробега между записями одометра
- [ ] Расчёт расхода л/100км между заправками
- [ ] График пробега по дням/неделям (recharts)
- [ ] График расхода топлива по заправкам

### Фаза 5 — Polish
*TODO: добавить критерии готовности для каждого шага*
*Цель: несколько машин, удобство, экспорт*

- [ ] UI переключения, добавления и переименования машин
- [ ] Редактирование и удаление записей
- [ ] Экспорт данных в CSV
